name: Build Binaries

on:
    push:
    pull_request:
        branches:
        - master

env:
    # Customize the CMake build type here (Release, Debug, RelWithDebInfo, etc.)
    BUILD_TYPE: Release
    GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:

    build_windows:
        if: ${{ !contains(github.ref_name, 'macos') }}
        runs-on: windows-2022

        steps:
        - uses: actions/checkout@v4

        - name: Create Build Environment
          run: cmake -E make_directory ${{runner.workspace}}/build

        - name: Download dependency cache
          shell: pwsh
          env:
            GH_TOKEN: ${{ github.token }}
          run: |
            New-Item -ItemType Directory ${{runner.workspace}}/cache -Force | Out-Null
            $null = gh release view build-cache --repo $env:GITHUB_REPOSITORY 2>&1
            if ($LASTEXITCODE -eq 0) {
              $null = gh release download build-cache `
                --repo $env:GITHUB_REPOSITORY `
                --pattern "*.zip" `
                --dir ${{runner.workspace}}/cache 2>&1
              if ($LASTEXITCODE -ne 0) { Write-Host "cache download failed, building from source" }
            } else {
              Write-Host "no build-cache release found, building from source"
            }
            Write-Host "cache:"; Get-ChildItem ${{runner.workspace}}/cache -ErrorAction SilentlyContinue | Select-Object Name,Length
            exit 0

        - name: Restore vcpkg binary cache
          shell: pwsh
          run: |
            $zip = "${{runner.workspace}}/cache/vcpkg-cache.zip"
            if (Test-Path $zip) {
              Expand-Archive $zip -DestinationPath C:/ -Force
              "CACHE_VCPKG=restored" | Out-File -FilePath $env:GITHUB_ENV -Append
              Write-Host "vcpkg binary cache restored - vcpkg install will reuse it incrementally"
            } else {
              "CACHE_VCPKG=missing" | Out-File -FilePath $env:GITHUB_ENV -Append
              Write-Host "vcpkg binary cache missing - building boost + friends from source"
            }
            $uhdZip = "${{runner.workspace}}/cache/uhd-3.15.0.0-x64.zip"
            if (Test-Path $uhdZip) {
              Expand-Archive $uhdZip -DestinationPath "C:/Program Files" -Force
              "CACHE_UHD=restored" | Out-File -FilePath $env:GITHUB_ENV -Append
              Write-Host "UHD restored to C:/Program Files/uhd"
            } else {
              "CACHE_UHD=missing" | Out-File -FilePath $env:GITHUB_ENV -Append
              Write-Host "UHD cache missing - build-uhd action will compile from source"
            }
            $pyZip = "${{runner.workspace}}/cache/uhd-python311-codegen.zip"
            if (Test-Path $pyZip) {
              Expand-Archive $pyZip -DestinationPath C:/dev -Force
              "CACHE_PY=restored" | Out-File -FilePath $env:GITHUB_ENV -Append
              Write-Host "Python 3.11 restored to C:/dev/py311_uhd"
            } else {
              "CACHE_PY=missing" | Out-File -FilePath $env:GITHUB_ENV -Append
              Write-Host "Python cache missing - will use setup-python 3.11"
            }

        - name: Restore MinGW toolchain
          shell: pwsh
          run: |
            $zip = "${{runner.workspace}}/cache/mingw64-codec2.zip"
            if (Test-Path $zip) {
              & 7z x $zip -oC:/ -y *> $null
              "CACHE_MINGW=restored" | Out-File -FilePath $env:GITHUB_ENV -Append
              Write-Host "MinGW toolchain restored to C:/msys64 - skipping pacman"
            } else {
              "CACHE_MINGW=missing" | Out-File -FilePath $env:GITHUB_ENV -Append
              Write-Host "MinGW cache missing - pacman will install toolchain"
            }

        - name: Download PothosSDR
          run: Invoke-WebRequest -Uri "https://downloads.myriadrf.org/builds/PothosSDR/PothosSDR-2020.01.26-vc14-x64.exe" -OutFile ${{runner.workspace}}/pothos.exe

        - name: Install PothosSDR
          run: mkdir "C:/Program Files/PothosSDR" ; 7z x ${{runner.workspace}}/pothos.exe -o"C:/Program Files/PothosSDR/"

        - name: Download libusb
          run: Invoke-WebRequest -Uri "https://github.com/libusb/libusb/releases/download/v1.0.23/libusb-1.0.23.7z" -OutFile ${{runner.workspace}}/libusb.7z

        - name: Patch Pothos with earlier libusb version
          working-directory: ${{runner.workspace}}
          run: 7z x libusb.7z -olibusb_old ; rm "C:/Program Files/PothosSDR/bin/libusb-1.0.dll" ; cp "libusb_old/MS64/dll/libusb-1.0.dll" "C:/Program Files/PothosSDR/bin/" ; rm "C:/Program Files/PothosSDR/lib/libusb-1.0.lib" ; cp "libusb_old/MS64/dll/libusb-1.0.lib" "C:/Program Files/PothosSDR/lib/"

        - name: Download librtlsdr
          run: Invoke-WebRequest -Uri "https://ftp.osmocom.org/binaries/windows/rtl-sdr/rtl-sdr-64bit-20240623.zip" -OutFile ${{runner.workspace}}/rtl-sdr.zip

        - name: Patch Pothos with newer librtlsdr version
          working-directory: ${{runner.workspace}}
          run: 7z x rtl-sdr.zip ; rm "C:/Program Files/PothosSDR/bin/rtlsdr.dll" ; cp "rtl-sdr-64bit-20240623/librtlsdr.dll" "C:/Program Files/PothosSDR/bin/rtlsdr.dll"

        - name: Download SDRPlay API
          run: Invoke-WebRequest -Uri "https://www.sdrpp.org/SDRplay.zip" -OutFile ${{runner.workspace}}/SDRplay.zip

        - name: Install SDRPlay API
          run: 7z x ${{runner.workspace}}/SDRplay.zip -o"C:/Program Files/"

        - name: Download codec2
          run: git clone https://github.com/drowe67/codec2

        - name: Prepare MinGW
          if: env.CACHE_MINGW != 'restored'
          run: C:/msys64/msys2_shell.cmd -defterm -here -no-start -mingw64 -c "pacman --noconfirm -S --needed base-devel mingw-w64-x86_64-toolchain mingw-w64-x86_64-cmake mingw-w64-x86_64-ninja"

        - name: Prepare build for codec2
          run: cd codec2 ; mkdir build ; cd build ; C:/msys64/msys2_shell.cmd -defterm -here -no-start -mingw64 -c "cmake .. -DCMAKE_GNUtoMS=ON -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_FLAGS=-static-libgcc"

        - name: Build codec2
          run: cd codec2/build ; C:/msys64/msys2_shell.cmd -defterm -here -no-start -mingw64 -c "ninja"

        - name: Install codec2
          run: mkdir "C:/Program Files/codec2" ; mkdir "C:/Program Files/codec2/include" ; mkdir "C:/Program Files/codec2/include/codec2" ; mkdir "C:/Program Files/codec2/lib" ; cd "codec2" ; xcopy "src" "C:/Program Files/codec2/include" ; cd "build" ; xcopy "src" "C:/Program Files/codec2/lib" ; xcopy "codec2" "C:/Program Files/codec2/include/codec2"

        - name: Install vcpkg dependencies (release x64 only, binary-cached)
          shell: pwsh
          env:
            VCPKG_BINARY_SOURCES: "files,C:/vcpkg-cache,readwrite"
          run: |
            try {
              git -C C:/vcpkg fetch origin 39344dff01c5a5a0134caf2624cdd492f05d30ea 2>$null
              git -C C:/vcpkg checkout 39344dff01c5a5a0134caf2624cdd492f05d30ea
              C:/vcpkg/bootstrap-vcpkg.bat -disableMetrics
              Write-Host "vcpkg pinned to cache-producing revision 39344dff01"
            } catch {
              Write-Host "vcpkg pin failed (best effort); using runner default"
            }
            $trip = "C:/vcpkg/triplets/x64-windows.cmake"
            if (-not (Select-String -Path $trip -Pattern "VCPKG_BUILD_TYPE" -Quiet)) {
              Add-Content -Path $trip -Value "set(VCPKG_BUILD_TYPE release)"
              Write-Host "x64-windows triplet forced to release-only (no debug build)"
            }
            $snap = 0
            if (Test-Path C:/vcpkg-cache) {
              $snap = (Get-ChildItem C:/vcpkg-cache -Recurse -File | Measure-Object -Property Length -Sum).Sum
            }
            vcpkg install fftw3:x64-windows glfw3:x64-windows portaudio:x64-windows zstd:x64-windows libusb:x64-windows itpp:x64-windows spdlog:x64-windows boost:x64-windows
            $now = 0
            if (Test-Path C:/vcpkg-cache) {
              $now = (Get-ChildItem C:/vcpkg-cache -Recurse -File | Measure-Object -Property Length -Sum).Sum
            }
            "CACHE_VCPKG_CHANGED=" + (($now -ne $snap) -or ($snap -eq 0)) | Out-File -FilePath $env:GITHUB_ENV -Append

        - name: Setup Python 3.11 (UHD codegen)
          id: setup-python
          if: env.CACHE_UHD != 'restored'
          uses: actions/setup-python@v5
          with:
            python-version: '3.11'

        - name: Build UHD 3.15.0.0
          if: env.CACHE_UHD != 'restored'
          uses: ./.github/actions/build-uhd

        - name: Upload dependency cache
          if: always() && github.event_name == 'push' && (env.CACHE_VCPKG_CHANGED == 'True' || env.CACHE_UHD == 'missing' || env.CACHE_PY == 'missing' || env.CACHE_MINGW == 'missing')
          shell: pwsh
          env:
            GH_TOKEN: ${{ github.token }}
          run: |
            $7z = "C:\Program Files\7-Zip\7z.exe"
            $cache = "${{runner.workspace}}/cache"
            if (Test-Path C:/vcpkg-cache) {
              & $7z a -tzip -mmt4 -mx5 "$cache/vcpkg-cache.zip" C:/vcpkg-cache *> "$cache/zip_cache.log"
              Write-Host "zipped vcpkg binary cache"
            }
            if (Test-Path "C:/Program Files/uhd") {
              & $7z a -tzip -mmt4 -mx5 "$cache/uhd-3.15.0.0-x64.zip" "C:/Program Files/uhd" *> "$cache/zip_uhd.log"
              Write-Host "zipped UHD install"
            }
            if (Test-Path "C:/msys64/msys2_shell.cmd") {
              & C:/msys64/msys2_shell.cmd -defterm -here -no-start -mingw64 -c "pacman -Scc --noconfirm"
              Push-Location C:/
              & $7z a -tzip -mmt4 -mx5 "$cache/mingw64-codec2.zip" msys64 *> "$cache/zip_mingw.log"
              Pop-Location
              Write-Host "zipped full MinGW tree (pacman cache cleaned)"
            }
            $pyDir = "C:/dev/py311_uhd"
            $hasDistutils = python -c "import distutils" 2>$null
            if ($LASTEXITCODE -eq 0) {
              if (-not (Test-Path "$pyDir/python.exe")) {
                $pref = python -c "import sys; print(sys.exec_prefix)"
                New-Item -ItemType Directory C:/dev -Force | Out-Null
                Copy-Item $pref $pyDir -Recurse -Force
                Write-Host "copied python $pref -> $pyDir"
              }
            } else {
              Write-Host "current python has no distutils - skipping python cache"
            }
            if (Test-Path "$pyDir/python.exe") {
              & $7z a -tzip -mmt4 -mx5 "$cache/uhd-python311-codegen.zip" $pyDir *> "$cache/zip_py.log"
              Write-Host "zipped python codegen env"
            }
            gh release view build-cache --repo $env:GITHUB_REPOSITORY 2>$null | Out-Null
            if ($LASTEXITCODE -ne 0) {
              gh release create build-cache --repo $env:GITHUB_REPOSITORY --title "Windows build cache" --notes "CI-built incremental dependency cache (vcpkg binary cache, UHD 3.15, python 3.11)." 2>$null
            }
            $toUpload = @()
            foreach ($z in @("vcpkg-cache.zip","mingw64-codec2.zip","uhd-3.15.0.0-x64.zip","uhd-python311-codegen.zip")) {
              if (Test-Path "$cache/$z") { $toUpload += "$cache/$z" } else { Write-Host "skip upload of missing $z" }
            }
            if ($toUpload.Count -gt 0) {
              gh release upload build-cache --repo $env:GITHUB_REPOSITORY --clobber @($toUpload)
              Write-Host "dependency cache uploaded to build-cache release"
            } else {
              Write-Host "nothing to upload"
            }

        - name: Install rtaudio
          run: git clone https://github.com/thestk/rtaudio ; cd rtaudio ; git checkout 2f2fca4502d506abc50f6d4473b2836d24cfb1e3 ; mkdir build ; cd build ; cmake .. ; cmake --build . --config Release ; cmake --install .

        - name: Install libperseus-sdr
          run: git clone https://github.com/AlexandreRouma/libperseus-sdr ; cd libperseus-sdr ; mkdir build ; cd build ; cmake -DCMAKE_BUILD_TYPE=Release "-DLIBUSB_LIBRARIES=C:/Program Files/PothosSDR/lib/libusb-1.0.lib" "-DLIBUSB_INCLUDE_DIRS=C:/Program Files/PothosSDR/include/libusb-1.0" .. "-DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake" ; cmake --build . --config Release  ; mkdir "C:/Program Files/PothosSDR/include/perseus-sdr" ; cp Release/perseus-sdr.dll "C:/Program Files/PothosSDR/bin" ; cp Release/perseus-sdr.lib "C:/Program Files/PothosSDR/bin" ; cd .. ; xcopy "src" "C:/Program Files/PothosSDR/include/perseus-sdr"

        - name: Install librfnm
          run: git clone https://github.com/AlexandreRouma/librfnm ; cd librfnm ; mkdir build ; cd build ; cmake .. -DCMAKE_BUILD_TYPE=Release "-DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake" ; cmake --build . --config Release ; cmake --install .

        - name: Install libfobos
          run: git clone https://github.com/AlexandreRouma/libfobos ; cd libfobos ; mkdir build ; cd build ; cmake .. -DCMAKE_BUILD_TYPE=Release "-DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake" ; cmake --build . --config Release ; cmake --install .

        - name: Install libhydrasdr
          run: git clone https://github.com/hydrasdr/rfone_host; cd rfone_host/ ; mkdir build ; cd build ; cmake .. -DCMAKE_BUILD_TYPE=Release "-DLIBUSB_LIBRARIES=C:\Program Files\PothosSDR\lib\libusb-1.0.lib" "-DLIBUSB_INCLUDE_DIR=C:\Program Files\PothosSDR\include\libusb-1.0" "-DTHREADS_PTHREADS_WIN32_LIBRARY=C:\Program Files\PothosSDR\lib\pthreadVC2.lib" "-DTHREADS_PTHREADS_INCLUDE_DIR=C:\Program Files\PothosSDR\include" -DCMAKE_INSTALL_PREFIX="C:/Program Files/hydrasdr-host" ; cmake --build . --config Release ; cmake --install .

        - name: Prepare CMake
          working-directory: ${{runner.workspace}}/build
          run: cmake -DCOPY_MSVC_REDISTRIBUTABLES=ON "$Env:GITHUB_WORKSPACE" "-DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake" -DOPT_BUILD_BLADERF_SOURCE=ON -DOPT_BUILD_LIMESDR_SOURCE=ON -DOPT_BUILD_PERSEUS_SOURCE=ON -DOPT_BUILD_SDRPLAY_SOURCE=ON -DOPT_BUILD_NEW_PORTAUDIO_SINK=ON -DOPT_BUILD_M17_DECODER=ON -DOPT_BUILD_RFNM_SOURCE=ON -DOPT_BUILD_FOBOSSDR_SOURCE=ON -DOPT_BUILD_HYDRASDR_SOURCE=ON -DOPT_BUILD_CH_EXTRAVHF_DECODER=ON -DOPT_BUILD_SOAPY_SOURCE=ON -DOPT_BUILD_MPEG_ADTS_SINK=OFF -DOPT_BUILD_USRP_SOURCE=ON -DUHD_DIR="C:/Program Files/uhd"

        - name: Build
          working-directory: ${{runner.workspace}}/build
          run: cmake --build . --config RelWithDebInfo --verbose

        - name: Create Archive
          working-directory: ${{runner.workspace}}
          run: '&($Env:GITHUB_WORKSPACE + "/make_windows_package.ps1") ./build ($Env:GITHUB_WORKSPACE + "/root")'

        - name: Save Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_windows_x64
              path: ${{runner.workspace}}/sdrpp_windows_x64.zip

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_windows_x64.zip -R ${{github.repository}} --clobber
    build_macos_intel:
      if: ${{ !contains(github.ref_name, 'windows') }}
      runs-on: macos-15-intel

      steps:
        - uses: actions/checkout@v4

        - name: Create Build Environment
          run: cmake -E make_directory ${{runner.workspace}}/build

        - name: Install dependencies
          run: HOMEBREW_NO_INSTALLED_DEPENDENTS_CHECK=1 brew install pkg-config libusb fftw glfw airspy airspyhf portaudio hackrf libbladerf codec2 zstd autoconf automake libtool spdlog boost && pip3 install mako --break-system-packages

        - name: Download dependency cache
          shell: bash
          env:
            GH_TOKEN: ${{ github.token }}
          run: |
            ARCH=$(uname -m); [ "$ARCH" = "arm64" ] && ARCH=arm64 || ARCH=x64
            mkdir -p ${{runner.workspace}}/cache
            if gh release view build-cache --repo ${{github.repository}} >/dev/null 2>&1; then
              gh release download build-cache --repo ${{github.repository}} \
                --pattern "uhd-3.15.0.0-macos-${ARCH}.zip" \
                --dir ${{runner.workspace}}/cache || echo "cache download failed, building from source"
            else
              echo "no build-cache release found, building from source"
            fi
            ls -la ${{runner.workspace}}/cache 2>/dev/null || true

        - name: Restore UHD cache
          shell: bash
          run: |
            ARCH=$(uname -m); [ "$ARCH" = "arm64" ] && ARCH=arm64 || ARCH=x64
            ZIP="${{runner.workspace}}/cache/uhd-3.15.0.0-macos-${ARCH}.zip"
            PREFIX="$(brew --prefix)"
            if [ -f "$ZIP" ]; then
              sudo unzip -o -q "$ZIP" -d "$PREFIX"
              echo "CACHE_UHD=restored" >> $GITHUB_ENV
              echo "UHD restored to $PREFIX/uhd"
            else
              echo "CACHE_UHD=missing" >> $GITHUB_ENV
              echo "UHD cache missing - build-uhd-macos action will compile from source"
            fi

        - name: Install volk
          run: git clone --recursive https://github.com/gnuradio/volk && cd volk && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        - name: Install SDRplay API
          run: wget https://www.sdrplay.com/software/SDRplayAPI-macos-installer-universal-3.15.0.pkg && sudo installer -pkg SDRplayAPI-macos-installer-universal-3.15.0.pkg -target /

        - name: Install libiio
          run: wget https://github.com/analogdevicesinc/libiio/archive/refs/tags/v0.25.zip && 7z x v0.25.zip && cd libiio-0.25 && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        - name: Install libad9361
          run: git clone https://github.com/analogdevicesinc/libad9361-iio && sed -i '' 's/VERSION 2.8.12/VERSION 3.5/' libad9361-iio/CMakeLists.txt && cd libad9361-iio && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        - name: Install LimeSuite
          run: git clone https://github.com/myriadrf/LimeSuite && cd LimeSuite && mkdir builddir && cd builddir && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        - name: Install librfnm
          run: git clone https://github.com/AlexandreRouma/librfnm && cd librfnm && mkdir build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make && sudo make install && cd ..

        - name: Install libfobos
          run: git clone https://github.com/AlexandreRouma/libfobos && cd libfobos && mkdir build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make && sudo make install && cd ..

        - name: Install more recent librtlsdr
          run: git clone https://github.com/osmocom/rtl-sdr && cd rtl-sdr && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 LIBRARY_PATH=$(pkg-config --libs-only-L libusb-1.0 | sed 's/\-L//') && sudo make install && cd ../../

        - name: Install libhydrasdr
          run: git clone https://github.com/hydrasdr/rfone_host && cd rfone_host && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        - name: Install libdlcr
          run: wget https://dragnlabs.com/host-tools/dlcr_host_v0.3.0.zip && mkdir dlcr_host && cd dlcr_host && 7z x ../dlcr_host_v0.3.0.zip && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 .. -DCMAKE_BUILD_TYPE=Release && make -j2 && sudo make install && cd ../../

#        - name: Some debugging
#          run: echo "before" && find /opt/homebrew/Cellar/codec2/1.2.0/include -print && echo "#ifndef CODEC2_HAVE_VERSION\n#define CODEC2_HAVE_VERSION\n#define CODEC2_VERSION_MAJOR 1\n#define CODEC2_VERSION_MINOR 2\n#define CODEC2_VERSION \"1.2.0\"\n#endif\n" > /opt/homebrew/Cellar/codec2/1.2.0/include/codec2/version.h && echo "after"; find /opt/homebrew/Cellar/codec2/1.2.0/include -print
#
        - name: Setup Python 3.11 (UHD codegen)
          id: setup-python
          if: env.CACHE_UHD != 'restored'
          uses: actions/setup-python@v5
          with:
            python-version: '3.11'

        - name: Build UHD 3.15.0.0
          if: env.CACHE_UHD != 'restored'
          uses: ./.github/actions/build-uhd-macos
          with:
            python-executable: ${{ steps.setup-python.outputs.pythonLocation }}/bin/python3

        - name: Upload dependency cache
          if: always() && github.event_name == 'push' && env.CACHE_UHD == 'missing'
          shell: bash
          env:
            GH_TOKEN: ${{ github.token }}
          run: |
            ARCH=$(uname -m); [ "$ARCH" = "arm64" ] && ARCH=arm64 || ARCH=x64
            PREFIX="$(brew --prefix)"
            if [ -d "$PREFIX/uhd" ]; then
              cd "$PREFIX"
              zip -rq "${{runner.workspace}}/cache/uhd-3.15.0.0-macos-${ARCH}.zip" uhd
              if ! gh release view build-cache --repo ${{github.repository}} >/dev/null 2>&1; then
                gh release create build-cache --repo ${{github.repository}} --title "macOS build cache" --notes "CI-built incremental dependency cache (UHD 3.15)." >/dev/null 2>&1 || true
              fi
              gh release upload build-cache --repo ${{github.repository}} --clobber "${{runner.workspace}}/cache/uhd-3.15.0.0-macos-${ARCH}.zip"
              echo "UHD cache uploaded"
            else
              echo "no UHD install found, nothing to cache"
            fi

        - name: Prepare CMake
          working-directory: ${{runner.workspace}}/build
          run: cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 $GITHUB_WORKSPACE -DOPT_BUILD_PLUTOSDR_SOURCE=ON -DOPT_BUILD_BLADERF_SOURCE=ON -DOPT_BUILD_SDRPLAY_SOURCE=ON -DOPT_BUILD_LIMESDR_SOURCE=ON -DOPT_BUILD_AUDIO_SINK=OFF -DOPT_BUILD_PORTAUDIO_SINK=ON -DOPT_BUILD_NEW_PORTAUDIO_SINK=ON -DOPT_BUILD_M17_DECODER=ON -DOPT_BUILD_PERSEUS_SOURCE=OFF -DOPT_BUILD_AUDIO_SOURCE=OFF -DUSE_BUNDLE_DEFAULTS=ON -DOPT_BUILD_RFNM_SOURCE=ON -DOPT_BUILD_FOBOSSDR_SOURCE=ON -DOPT_BUILD_DRAGONLABS_SOURCE=ON -DOPT_BUILD_CH_EXTRAVHF_DECODER=ON -DOPT_BUILD_USRP_SOURCE=ON "-DCMAKE_PREFIX_PATH=$(brew --prefix)/uhd" -DCMAKE_BUILD_TYPE=Release

        - name: Build
          working-directory: ${{runner.workspace}}/build
          run: make VERBOSE=1 -j3

        - name: Create Archive
          working-directory: ${{runner.workspace}}
          run: cd $GITHUB_WORKSPACE && sh make_macos_bundle.sh ${{runner.workspace}}/build ./SDR++Brown.app && zip -r ${{runner.workspace}}/sdrpp_macos_intel.zip SDR++Brown.app

        - name: Save Archive
          uses: actions/upload-artifact@v4
          with:
            name: sdrpp_macos_intel
            path: ${{runner.workspace}}/sdrpp_macos_intel.zip

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_macos_intel.zip -R ${{github.repository}} --clobber



    build_macos_arm:
        if: ${{ !contains(github.ref_name, 'windows') }}
        runs-on: macos-15

        steps:
        - uses: actions/checkout@v4
        
        - name: Create Build Environment
          run: cmake -E make_directory ${{runner.workspace}}/build

        - name: Install dependencies
          run: brew install pkg-config libusb fftw glfw airspy airspyhf portaudio hackrf libbladerf codec2 zstd autoconf automake libtool spdlog boost && pip3 install mako --break-system-packages

        - name: Download dependency cache
          shell: bash
          env:
            GH_TOKEN: ${{ github.token }}
          run: |
            ARCH=$(uname -m); [ "$ARCH" = "arm64" ] && ARCH=arm64 || ARCH=x64
            mkdir -p ${{runner.workspace}}/cache
            if gh release view build-cache --repo ${{github.repository}} >/dev/null 2>&1; then
              gh release download build-cache --repo ${{github.repository}} \
                --pattern "uhd-3.15.0.0-macos-${ARCH}.zip" \
                --dir ${{runner.workspace}}/cache || echo "cache download failed, building from source"
            else
              echo "no build-cache release found, building from source"
            fi
            ls -la ${{runner.workspace}}/cache 2>/dev/null || true

        - name: Restore UHD cache
          shell: bash
          run: |
            ARCH=$(uname -m); [ "$ARCH" = "arm64" ] && ARCH=arm64 || ARCH=x64
            ZIP="${{runner.workspace}}/cache/uhd-3.15.0.0-macos-${ARCH}.zip"
            PREFIX="$(brew --prefix)"
            if [ -f "$ZIP" ]; then
              sudo unzip -o -q "$ZIP" -d "$PREFIX"
              echo "CACHE_UHD=restored" >> $GITHUB_ENV
              echo "UHD restored to $PREFIX/uhd"
            else
              echo "CACHE_UHD=missing" >> $GITHUB_ENV
              echo "UHD cache missing - build-uhd-macos action will compile from source"
            fi

        - name: Install volk
          run: git clone --recursive https://github.com/gnuradio/volk && cd volk && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        - name: Install SDRplay API
          run: wget https://www.sdrplay.com/software/SDRplayAPI-macos-installer-universal-3.15.0.pkg && sudo installer -pkg SDRplayAPI-macos-installer-universal-3.15.0.pkg -target /

        - name: Install libiio
          run: wget https://github.com/analogdevicesinc/libiio/archive/refs/tags/v0.25.zip && 7z x v0.25.zip && cd libiio-0.25 && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        - name: Install libad9361
          run: git clone https://github.com/analogdevicesinc/libad9361-iio && cd libad9361-iio && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release -DCMAKE_POLICY_VERSION_MINIMUM=3.5 .. && make -j3 && sudo make install && cd ../../

        - name: Install LimeSuite
          run: git clone https://github.com/myriadrf/LimeSuite && cd LimeSuite && mkdir builddir && cd builddir && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        # - name: Install libperseus
        #   run: git clone https://github.com/Microtelecom/libperseus-sdr && cd libperseus-sdr && autoreconf -i && ./configure --prefix=/usr/local && make && make install && cd ..

        - name: Install librfnm
          run: git clone https://github.com/AlexandreRouma/librfnm && cd librfnm && mkdir build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make && sudo make install && cd ..

        - name: Install libfobos
          run: git clone https://github.com/AlexandreRouma/libfobos && cd libfobos && mkdir build && cd build && cmake .. -DCMAKE_BUILD_TYPE=Release && make && sudo make install && cd ..

        - name: Install more recent librtlsdr
          run: git clone https://github.com/osmocom/rtl-sdr && cd rtl-sdr && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 LIBRARY_PATH=$(pkg-config --libs-only-L libusb-1.0 | sed 's/\-L//') && sudo make install && cd ../../

        - name: Install libhydrasdr
          run: git clone https://github.com/hydrasdr/rfone_host && cd rfone_host && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 -DCMAKE_BUILD_TYPE=Release .. && make -j3 && sudo make install && cd ../../

        - name: Install libdlcr
          run: wget https://dragnlabs.com/host-tools/dlcr_host_v0.3.0.zip && mkdir dlcr_host && cd dlcr_host && 7z x ../dlcr_host_v0.3.0.zip && mkdir build && cd build && cmake -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 .. -DCMAKE_BUILD_TYPE=Release && make -j2 && sudo make install && cd ../../

        - name: Setup Python 3.11 (UHD codegen)
          id: setup-python
          if: env.CACHE_UHD != 'restored'
          uses: actions/setup-python@v5
          with:
            python-version: '3.11'

        - name: Build UHD 3.15.0.0
          if: env.CACHE_UHD != 'restored'
          uses: ./.github/actions/build-uhd-macos
          with:
            python-executable: ${{ steps.setup-python.outputs.pythonLocation }}/bin/python3

        - name: Upload dependency cache
          if: always() && github.event_name == 'push' && env.CACHE_UHD == 'missing'
          shell: bash
          env:
            GH_TOKEN: ${{ github.token }}
          run: |
            ARCH=$(uname -m); [ "$ARCH" = "arm64" ] && ARCH=arm64 || ARCH=x64
            PREFIX="$(brew --prefix)"
            if [ -d "$PREFIX/uhd" ]; then
              cd "$PREFIX"
              zip -rq "${{runner.workspace}}/cache/uhd-3.15.0.0-macos-${ARCH}.zip" uhd
              if ! gh release view build-cache --repo ${{github.repository}} >/dev/null 2>&1; then
                gh release create build-cache --repo ${{github.repository}} --title "macOS build cache" --notes "CI-built incremental dependency cache (UHD 3.15)." >/dev/null 2>&1 || true
              fi
              gh release upload build-cache --repo ${{github.repository}} --clobber "${{runner.workspace}}/cache/uhd-3.15.0.0-macos-${ARCH}.zip"
              echo "UHD cache uploaded"
            else
              echo "no UHD install found, nothing to cache"
            fi

        - name: Prepare CMake
          working-directory: ${{runner.workspace}}/build
          run: cmake -DCMAKE_POLICY_VERSION_MINIMUM=3.5 -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 $GITHUB_WORKSPACE -DOPT_BUILD_PLUTOSDR_SOURCE=ON -DOPT_BUILD_BLADERF_SOURCE=ON -DOPT_BUILD_SDRPLAY_SOURCE=ON -DOPT_BUILD_LIMESDR_SOURCE=ON -DOPT_BUILD_AUDIO_SINK=OFF -DOPT_BUILD_PORTAUDIO_SINK=ON -DOPT_BUILD_NEW_PORTAUDIO_SINK=ON -DOPT_BUILD_M17_DECODER=OFF -DOPT_BUILD_PERSEUS_SOURCE=OFF -DOPT_BUILD_AUDIO_SOURCE=OFF -DOPT_BUILD_RFNM_SOURCE=ON -DOPT_BUILD_FOBOSSDR_SOURCE=ON -DOPT_BUILD_HYDRASDR_SOURCE=ON -DOPT_BUILD_DRAGONLABS_SOURCE=ON -DUSE_BUNDLE_DEFAULTS=ON -DCMAKE_BUILD_TYPE=Release -DOPT_BUILD_CH_EXTRAVHF_DECODER=ON -DOPT_BUILD_USRP_SOURCE=ON "-DCMAKE_PREFIX_PATH=$(brew --prefix)/uhd"

        - name: Build
          working-directory: ${{runner.workspace}}/build
          run: make VERBOSE=1 -j3

        - name: Create Archive
          working-directory: ${{runner.workspace}}
          run: cd $GITHUB_WORKSPACE && sh make_macos_bundle.sh ${{runner.workspace}}/build ./SDR++Brown.app && zip -r ${{runner.workspace}}/sdrpp_macos_arm.zip SDR++Brown.app

        - name: Save Archive
          uses: actions/upload-artifact@v4
          with:
            name: sdrpp_macos_arm
            path: ${{runner.workspace}}/sdrpp_macos_arm.zip

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_macos_arm.zip -R ${{github.repository}} --clobber



    build_debian_bullseye_amd64:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4

        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/debian_bullseye && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_debian_bullseye_amd64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_debian_bullseye_amd64
              path: ${{runner.workspace}}/sdrpp_debian_bullseye_amd64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_debian_bullseye_amd64.deb -R ${{github.repository}} --clobber


    build_debian_bookworm:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4
        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/debian_bookworm && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_debian_bookworm_amd64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
            name: sdrpp_debian_bookworm_amd64
            path: ${{runner.workspace}}/sdrpp_debian_bookworm_amd64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_debian_bookworm_amd64.deb -R ${{github.repository}} --clobber

    build_ubuntu_jammy_amd64:
      if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
      runs-on: ubuntu-latest

      steps:
        - uses: actions/checkout@v4

        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/ubuntu_jammy && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_ubuntu_jammy_amd64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
            name: sdrpp_ubuntu_jammy_amd64
            path: ${{runner.workspace}}/sdrpp_ubuntu_jammy_amd64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_ubuntu_jammy_amd64.deb -R ${{github.repository}} --clobber

    build_ubuntu_jammy_aarch64:
      if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
      runs-on: ubuntu-22.04-arm

      steps:
        - uses: actions/checkout@v4

        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/ubuntu_jammy && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_ubuntu_jammy_aarch64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
            name: sdrpp_ubuntu_jammy_aarch64
            path: ${{runner.workspace}}/sdrpp_ubuntu_jammy_aarch64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_ubuntu_jammy_aarch64.deb -R ${{github.repository}} --clobber

    build_debian_trixie_amd64:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4
        
        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/debian_trixie && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_debian_trixie_amd64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_debian_trixie_amd64
              path: ${{runner.workspace}}/sdrpp_debian_trixie_amd64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_debian_trixie_amd64.deb -R ${{github.repository}} --clobber

    build_debian_trixie_aarch64:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-24.04-arm

        steps:
        - uses: actions/checkout@v4
        
        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/debian_trixie && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_debian_trixie_aarch64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_debian_trixie_aarch64
              path: ${{runner.workspace}}/sdrpp_debian_trixie_aarch64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_debian_trixie_aarch64.deb -R ${{github.repository}} --clobber

    build_debian_sid_amd64:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4
        
        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/debian_sid && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_debian_sid_amd64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_debian_sid_amd64
              path: ${{runner.workspace}}/sdrpp_debian_sid_amd64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_debian_sid_amd64.deb -R ${{github.repository}} --clobber

    build_debian_sid_aarch64:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-24.04-arm

        steps:
        - uses: actions/checkout@v4
        
        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/debian_sid && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_debian_sid_aarch64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_debian_sid_aarch64
              path: ${{runner.workspace}}/sdrpp_debian_sid_aarch64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_debian_sid_aarch64.deb -R ${{github.repository}} --clobber

    build_ubuntu_focal_amd64:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4
        
        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/ubuntu_focal && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_ubuntu_focal_amd64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_ubuntu_focal_amd64
              path: ${{runner.workspace}}/sdrpp_ubuntu_focal_amd64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_ubuntu_focal_amd64.deb -R ${{github.repository}} --clobber

    build_ubuntu_focal_aarch64:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-24.04-arm

        steps:
        - uses: actions/checkout@v4
        
        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/ubuntu_focal && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_ubuntu_focal_aarch64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_ubuntu_focal_aarch64
              path: ${{runner.workspace}}/sdrpp_ubuntu_focal_aarch64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_ubuntu_focal_aarch64.deb -R ${{github.repository}} --clobber

    build_ubuntu_noble:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4

        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/ubuntu_noble && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_ubuntu_noble_amd64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_ubuntu_noble_amd64
              path: ${{runner.workspace}}/sdrpp_ubuntu_noble_amd64.deb
        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_ubuntu_noble_amd64.deb -R ${{github.repository}} --clobber

    build_ubuntu_noble_aarch64:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-24.04-arm

        steps:
        - uses: actions/checkout@v4

        - name: Create Docker Image
          run: cd $GITHUB_WORKSPACE/docker_builds/ubuntu_noble && docker build . --tag sdrpp_build

        - name: Run Container
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus --env BUILD_NO="-$GITHUB_RUN_NUMBER" sdrpp_build /root/do_build.sh

        - name: Recover Deb Archive
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/sdrpp_debian_amd64.deb ./sdrpp_ubuntu_noble_aarch64.deb

        - name: Save Deb Archive
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_ubuntu_noble_aarch64
              path: ${{runner.workspace}}/sdrpp_ubuntu_noble_aarch64.deb

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run: gh release upload rolling ${{runner.workspace}}/sdrpp_ubuntu_noble_aarch64.deb -R ${{github.repository}} --clobber

    build_android:
        if: ${{ !contains(github.ref_name, 'windows') && !contains(github.ref_name, 'macos') }}
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4

        - name: Fetch container
          working-directory: ${{runner.workspace}}
          run: git clone https://github.com/sannysanoff/android-sdr-kit

        - name: Build container
          working-directory: ${{runner.workspace}}/android-sdr-kit
          run: docker build --progress=plain -t android-sdr-kit .

        - name: Build
          run: docker run --name build -v $GITHUB_WORKSPACE:/root/SDRPlusPlus android-sdr-kit /bin/bash -l -c "apt-get update && apt-get install -y unzip && cd /root/SDRPlusPlus/android && gradle -PsdrKitRoot=/sdr-kit --info assembleDebug"

        - name: Recover APK
          working-directory: ${{runner.workspace}}
          run: docker cp build:/root/SDRPlusPlus/android/app/build/outputs/apk/debug/app-debug.apk ./ && mv app-debug.apk sdrpp.apk

        - name: Save APK
          uses: actions/upload-artifact@v4
          with:
              name: sdrpp_android
              path: ${{runner.workspace}}/sdrpp.apk

        - name: Update Nightly
          if: github.ref == 'refs/heads/master'
          run:  gh release upload rolling ${{runner.workspace}}/sdrpp.apk -R ${{github.repository}} --clobber


    check_spelling:
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4

        - name: Install codespell
          run: sudo apt update -y && sudo apt install -y codespell

        - name: Running codespell
          run: cd $GITHUB_WORKSPACE && codespell -q 2 || true

    check_formatting:
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4

        - name: Run check_clang_format
          run: cd $GITHUB_WORKSPACE && chmod +x ./check_clang_format.sh && ./check_clang_format.sh || true
